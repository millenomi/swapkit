#include "../Mac/Common.h"

#if defined(TARGET_IPHONE_SIMULATOR) && defined(kL0iPhoneSimulatorPretendIsiPodTouch)
#warning Infinite Labs code in the appication will pretend being run on an iPod touch while in the simulator.
#endif
